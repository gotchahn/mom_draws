= render "sidebar"

.col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.main
  %h1.page-header
    %span.fa.fa-gift.fa-lg
    Patrocinadores

  = render partial: "shared/notice_for"

  .row
    - if @sponsors.any?
      .table-responsive
        %table.table.table-striped
          %thead
            %tr
              %th Código
              %th Nombre
              %th Email
              %th Teléfono
          %tbody
            = render @sponsors
    - else
      %em No se ha agregado ningun patrocinador.


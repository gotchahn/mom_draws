= render "sidebar"

.col-sm-9.col-sm-offset-3.col-md-10.col-md-offset-2.main
  %h1.page-header Eventos

  = render partial: "shared/notice_for"
  = render partial: "recents", locals: { recents: @recents }

  - if @events.any?

    %h2.sub-header Otros Eventos
    .row.placeholders
      .table-responsive
        %table.table.table-striped
          %thead
            %tr
              %th Código
              %th Titulo
              %th Meta
              %th Recaudado
              %th Estado
          %tbody
            = render @events
